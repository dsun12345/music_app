<%= form_for @song do |f| %>
    
    <%= render partial: "/partials/errors", locals: {obj: @song} %>

    <%= f.label :title %>
    <%= f.text_field :title %> 

    <%= f.label :artist %>
    <%= f.text_field :artist %> 

    <%= f.submit "Edit Song!!" %> 
<% end %> 